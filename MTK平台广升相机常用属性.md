# MTK 平台广升相机常用属性

MTK 平台广升相机常用属性，可通过 `adb shell setprop name value` 命令设置系统属性，程序读到后便会改变逻辑，向预期的方向运行。  
可通过 `adb shell getprop name` 命令读取系统属性，查看当前属性的值。  
属性名规则小知识：  
* `ro.` 开头的属性名是 readonly 的缩写，代表只读，如果通过 getprop 可以读到，设置将会失败。  
  所以该属性如果系统出厂没有内置，则可以设置一次。  
  不会随设备重启而被清除（想要清除需要恢复出厂）。  
* `persist.` 开头的代表持久化属性。  
  可以重复设置。  
  不会随设备重启而被清除。    
* 其他开头的可以重复设置。  
  会随设备重启而被清除。（如果重启设备后，这类属性通常要重新设置）  

### 打开照片模式的美颜效果
_仅对 2025-04-30 以后的项目有效，App 内实现_  

仅对前摄生效  
**`persist.camera.1.photo_fb_level`** 2,3,3,0,0,0,0

仅对后摄生效  
**`persist.camera.0.photo_fb_level`** 2,3,3,0,0,0,0

对前后摄都生效，应用权重比上面的低  
**`persist.camera.photo_fb_level`** 2,3,3,0,0,0,0

第一位：2，固定 2，如果需要关闭照片美颜，请设置为 0，会忽略后面的值  
第二位：0-5，磨皮  
第三位：0-5，美白  
第四位：0-5，瘦脸  
第五位：0-5，大眼  
第六位：待扩展，固定 0  
第七位：待扩展，固定 0  
